export default function HomePage() {
  return (
    <main style={{ fontFamily: "system-ui, sans-serif", padding: "2rem", maxWidth: 720 }}>
      <h1>Synthetic Harness Lab</h1>
      <p>
        The evidence viewer ships in a later milestone. For now the keyless sweep
        runs from the command line: <code>npm run sweep</code>.
      </p>
    </main>
  );
}
